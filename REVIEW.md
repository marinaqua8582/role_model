# role_model 점검 및 수정 보고서

## 작업 상태

수정본은 로컬 산출물입니다. GitHub push, PR 생성, Vercel 배포, 운영 Apps Script 변경, 실제 Google Sheets 읽기·쓰기는 수행하지 않았습니다.
기준 소스: marinaqua8582/role_model, main 커밋 `16ea7a86f3c6b7b0cb9b18ff19c6623c17a69ad6`.
Git 직접 다운로드가 연결 오류로 실패하여 GitHub 연동으로 해당 커밋의 전체 파일을 확보했습니다.

## 발견한 원인

1. **중복 생성 가능성**: Apps Script는 이미 기존 행 조회 후 update/append를 수행하지만 LockService가 없었습니다. 두 요청이 동시에 기존 행 없음으로 판단하면 각각 append할 수 있습니다. 실제 운영 시트와 배포된 GAS 버전은 확인하지 않았으므로, 관찰된 중복 행의 발생 원인을 확정한 것은 아닙니다.
2. **키와 이름 처리**: studentKey 생성이 여러 곳에 반복되어 있고, Tests/Submissions 저장은 문자열 완전 일치에 의존했습니다. Apps Script 템플릿의 일부 공백 정규식은 바깥 문자열의 이스케이프가 빠져 생성된 코드에서 의도대로 작동하지 않았습니다.
3. **중복 복원**: Progress/Tests/Submissions 조회에서 첫 번째 일치 행을 읽었습니다. 최근 자료 또는 다른 중복 행의 유효한 값이 누락될 수 있었습니다.
4. **저장 성공 오표시**: saveTests/updateRevision은 통신 예외 후 로컬 저장을 하고 성공을 반환했습니다. 단계 이동의 saveStudentProgress는 Vercel에 존재하지 않는 `/api/student/save-step`을 호출한 뒤 실패해도 성공으로 표시했습니다. 이 경로의 Express 구현도 메모리 저장이어서 Google Sheets 저장 완료를 의미하지 않았습니다.
5. **제출 완료 선표시**: STEP10은 실제 submitFinal 성공 전에 부모의 완료 상태를 변경했습니다.
6. **복원 누락**: 직접 입력 항목, 요약 문장, STEP5 체크 상태, 테스트 메모 일부는 시트에 보내지 않거나 저장 열이 없어 다른 기기에서 복원되지 않았습니다.
7. **학생 간 조회**: 공개 loadProgress 요청은 studentKey만으로 진행 내용과 Google ID를 반환했습니다. RosterOptions 자체는 번호 선택지만 반환합니다. 통신 장애 시 로컬 명단·진행 내용만으로 로그인하는 fallback도 있었습니다.
8. **공용 기기**: 로그아웃은 현재 studentKey만 제거하고 진행 캐시를 남겼습니다.
9. **번역 설정**: HTML 언어가 영어였고 번역 제외 설정이 없었습니다.

## 수정한 파일 및 주요 변경 사항

| 파일 | 변경 |
|---|---|
| index.html | lang=ko, translate=no, Google notranslate 메타 태그 |
| src/utils/studentKey.ts (신규) | 숫자·문자열·공백·전각 숫자를 일관된 학년-반-번호로 정규화 |
| src/utils/gasScriptTemplate.ts | 저장 요청 잠금 및 flush/release, 키 정규화, 최근 중복 행 선택·같은 신원 빈 값 보완, 다른 행 삭제 금지, 현재 단계 부분 갱신, 진행 단계 하락 방지, 보조 JSON 저장·복원, 학생 세션 범위 확인 |
| src/api/client.ts | GAS 저장 성공을 기준으로 처리, 실패 시 오류 반환, 로컬 인증 fallback 제거, 서버 기준 복원, 로그아웃 캐시 제거, 세션 변경 후 늦은 응답 무효화, 단계 이동은 진행 번호만 저장 |
| src/App.tsx | 저장 완료 후 이동, 저장 중 학생 화면 조작 제한, 이전/탭 이동 시 해당 단계 저장, 실패 안내, 로그아웃 캐시·화면 초기화, 저장 안 된 입력의 이탈 안내 |
| src/components/student/Step10Submission.tsx | 서버 제출 성공 후에만 완료 상태 갱신 |
| api/gas-proxy.ts | 관리자 학생 삭제 요청도 공개 proxy 차단 목록에 포함 |
| server.ts | 하드코딩된 샘플 학생 fallback 명단 제거 |
| tests/gas.test.cjs, tests/client.test.cjs (신규) | 가상 시트·실제 클라이언트 함수·생성된 GAS 코드 통합 테스트, HTML 렌더링 검사 |

학생 로그인 입력은 학년·반·번호·이름 그대로입니다. 인증코드나 비밀번호 입력을 추가하지 않았습니다. Roster 확인 후 브라우저가 내부 세션 토큰을 보관하며, 학생은 토큰을 입력하거나 기억할 필요가 없습니다. 토큰은 최대 6시간 캐시에 보관되고 만료·캐시 소실 시 같은 방식으로 다시 로그인합니다. 본인 Google ID 전체 표시를 유지했습니다.

## 데이터 영향

- 실제 운영 데이터는 변경하지 않았습니다.
- 적용 후 Progress의 기존 열 뒤에 `stepData`, Tests의 기존 열 뒤에 `step8Data`가 필요할 때 추가됩니다. 기존 열 순서와 저장소 종류는 유지합니다.
- 신규 중복 생성을 잠금과 upsert로 방지합니다. 기존 중복 행은 자동 삭제하지 않습니다. 따라서 이미 2행인 학생을 적용 직후 물리적으로 1행으로 줄이는 작업은 하지 않습니다.
- 최근 저장 시각이 유효한 행을 우선하며, 같은 신원으로 판단되는 오래된 행에서 빈 셀을 보완합니다. 같은 시각이면 아래쪽 행을 우선합니다. 중복 행 간 이름·Google ID가 충돌하면 해당 행끼리는 병합하지 않습니다.
- 같은 학번에 다른 이름의 이전 자료가 있으면 덮어쓰지 않고 교사 확인을 요청합니다. 이미 존재하던 명시적 학생 초기화 기능은 유지되며, 자동 정리를 추가하지 않았습니다.
- 브라우저 로컬 진행 캐시는 로그아웃/새 로그인 시 지워집니다. 서버에 성공적으로 저장된 자료는 삭제하지 않습니다. 과거에 서버 저장 없이 로컬 캐시에만 남아 있던 자료는 먼저 별도 확인·보관해야 합니다.
- 소스의 3명짜리 샘플 fallback 명단을 제거했습니다. 실제 학생 명단인지 확인할 근거는 없었습니다. 정상 GAS Roster 로그인은 이 배열에 의존하지 않습니다. 관리자 입력 화면의 예시 문자열은 실제 등록 명단과 구분됩니다.

## 추가로 발견한 문제 및 제한

- 운영에 배포된 GAS가 저장소 템플릿과 같은 버전인지 확인하지 못했습니다. 기존 시트 헤더/열 배치도 실제 확인이 필요합니다. 기존 코드가 쓰는 표준 열 순서를 유지한 수정입니다.
- 관리자 대시보드의 기존 집계 구현은 유지했습니다. 기존 중복이 있는 학생은 대시보드 집계와 상세 자료를 대조해야 합니다. 이번 수정은 기존 중복을 물리적으로 정리하는 마이그레이션이 아닙니다.
- 기존 학년·반·번호·이름 로그인은 다른 학생의 정보를 알고 입력하는 사칭 자체를 방지하지 못합니다. 이번 조치는 로그인된 세션이 다른 studentKey를 임의 조회하는 경로를 막습니다.
- 기존 GAS는 ADMIN_API_SECRET이 없으면 관리자 요청을 허용했습니다. 이는 전체 명단 조회 우회 경로이므로 값이 없을 때 거부하도록 수정했습니다. 기존 비밀번호와 비밀값 자체를 변경하지 않았습니다.
- STEP10 작성 중인 내용은 최종 제출 성공 전까지 서버 제출 자료가 아닙니다. 이탈 경고를 적용했지만 미제출 초안의 자동 저장 기능을 새로 추가하지 않았습니다.
- 브라우저 번역 제외 속성은 브라우저에 전달하는 설정입니다. 모든 번역 확장 기능이나 강제 번역을 보장해서 차단하는 기능은 아닙니다.

## 배포 전 확인사항

1. 현재 Google Sheets와 Apps Script 코드를 백업하고, 먼저 복사한 시트/테스트 배포에서 검증합니다. 로컬 캐시에만 남은 과거 작성 자료도 적용 전에 확인합니다.
2. 현재 저장소 커밋이 기준 커밋과 달라졌으면 패치를 검토하여 적용합니다. 최신 변경을 ZIP으로 무조건 덮어쓰지 않습니다. `role_model-review.patch`는 기준 소스에 대한 변경과 신규 테스트를 포함합니다.
3. `GoogleAppsScript.gs`는 수정된 getGoogleAppsScriptCode()에서 직접 생성했습니다. 웹앱과 GAS를 함께 적용해야 합니다. 구버전 GAS 응답에는 내부 세션 토큰이 없어 새 웹앱 로그인이 안내 오류로 멈춥니다. 수업 중 교체하지 말고 점검 시간을 확보합니다.
4. GAS Script Properties의 `ADMIN_API_SECRET`과 Vercel 서버 환경변수의 동일 값 설정 여부를 확인합니다. 실제 비밀값은 채팅이나 소스에 적지 않습니다. 기존 `ADMIN_PASSWORD`는 유지합니다.
5. Vercel `GAS_URL` 및 사용하는 경우 `VITE_GAS_URL`이 의도한 동일 GAS 배포를 가리키는지 확인합니다. 기존 실행 주체/시트 접근 권한을 유지합니다. 관리자 비밀값을 VITE_ 변수에 넣지 않습니다.
6. 기존 런타임에서 `npm run lint`, `npm run build`, `node tests/client.test.cjs`를 실행합니다. 이 환경의 일반 build는 EPERM으로 완료하지 못했습니다.
7. 아래 7개 시나리오를 실제 테스트 시트·PC·휴대폰에서 수행하고 교사 관리자 화면의 반별/단계별 조회, 인쇄, 학생 추가·삭제까지 확인한 뒤 운영 반영합니다.

## 테스트 결과

실행 명령: `node tests/client.test.cjs` (GAS 테스트 포함). **13개 통과**. `npm run lint` 통과. `git diff --check` 통과.

| 요청 시나리오 | 직접 실행한 검증 | 미확인 범위 |
|---|---|---|
| 1 신규 학생 STEP1→2 | 실제 클라이언트 함수와 생성 GAS를 가상 시트에 연결, Progress 한 행 유지 | 실제 Google Sheets 및 브라우저 클릭 흐름 |
| 2 로그아웃·재로그인 복원 | 새 클라이언트 환경 로그인에서 STEP1/2와 직접 입력·STEP5 상태 복원 | 실제 다른 기기/모바일 화면 종료 |
| 3 기존 학생 STEP3 추가 | GAS 요청으로 STEP3 추가 후 기존 STEP1/2 유지 및 한 행 확인 | 운영 자료 |
| 4 빠른 중복 저장 | 반복 요청 10회 한 행 유지, 잠금 획득·해제 및 timeout 실패 검증 | 실제 GAS 동시 실행 부하 시험. 가상 테스트는 Google의 분산 잠금 자체를 검증하지 않음 |
| 5 같은 기기 A→B | 로그아웃 캐시 제거, 늦은 A 응답 무효화, B에 A 정보 없음 | 실제 UI 전환 클릭 |
| 6 Google ID | 권한 없는 다른 key 조회/저장 거부, STEP7 서버 렌더링 HTML에 본인 전체 주소만 포함 | 실제 브라우저/클립보드 조작 |
| 7 번역 방지 | HTML lang/translate/meta 설정 검사 | 실제 모바일 Chrome 자동 번역 동작 |

추가 실행: 네트워크·서버 실패 시 저장 성공 금지, 최종 제출 반복 시 한 행 유지, 중복 행 최신 값·빈 값 병합과 원본 행 보존, 관리자 API 명단 추가·삭제 및 기존 진행 자료 유지, 관리자 인쇄용 HTML 렌더링.

**미완료 검사:** `npm run build`는 esbuild 하위 프로세스 실행에서 `spawn EPERM`으로 종료되어 빌드 성공을 확인하지 못했습니다. 이 오류 이전에 TypeScript 검사는 통과했습니다. 브라우저 E2E, 인쇄 레이아웃, 운영 GAS/Sheets 연결, 실제 동시 접속 부하는 검증하지 않았습니다. 따라서 이 결과만으로 바로 운영 배포해도 된다고 판정하지 않습니다.
